<?php

use phpDocumentor\Reflection\Types\Boolean;

class Q26
{
    /**
     * 引数として与えられた文字列の最初の2文字が末尾の２文字と等しい時
     * trueを返す。それ以外はfalseを返す
     * 例：
     * 　frontAgain("edited") → true
     * 　frontAgain("edit") → false
     * 　frontAgain("ed") → true
     * 
     * @param string $str
     * 
     * @return bool 上記の条件判定に従ってbool値を返す
     * 
     */
    public function frontAgain(string $str) : bool
    {
       return '-1';
    }
}