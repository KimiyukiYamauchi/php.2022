<?php

class Q8
{
    /**
     * 与えられた文字列の先頭と末尾の文字を除いた文字列を返す
     * 与えれれる文字列は２文字以上
     * 例：
     * 　withoutEnd("Hello") → "ell"
     * 　withoutEnd("java") → "av"
     * 　withoutEnd("coding") → "odin"
     * 
     * @param string $str 文字列
     * 
     * @return string 上記の条件で作られた文字列
     * 
     */
    public function withoutEnd(string $str) : string
    {
        return '-1';
    }
}