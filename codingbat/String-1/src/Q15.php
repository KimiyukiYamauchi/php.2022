<?php

class Q15
{
    /**
     * 与えられた偶数長の文字列の中間の２文字を返す。例えば"string"が
     * 与えられた場合は"ri"を返す関数。与えられる文字列は２文字以上
     * 　middleTwo("string") → "ri"
     * 　middleTwo("code") → "od"
     * 　middleTwo("Practice") → "ct"
     * 
     * @param string $str
     * 
     * @return string 上記の処理を行った文字列を返す
     * 
     */
    public function middleTwo(string $str) : string
    {
        return '-1';
    }
}