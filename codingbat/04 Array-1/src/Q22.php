<?php

use phpDocumentor\Reflection\Types\Boolean;

class Q22
{
    /**
     * 引数として要素数が基数のint型の配列が与えれる
     * 配列の先頭、真ん中、末尾の要素で、一番大きな値を返す
     * 配列の要素数は1以上
     * 例：
     * 　maxTriple([1, 2, 3]) → 3
     * 　maxTriple([1, 5, 3]) → 5
     * 　maxTriple([5, 2, 3]) → 5
     * 
     * @param int [] $nums
     * 
     * @return int 上記処理の結果
     * 
     */
    public function maxTriple(array $nums) : int
    {
        return -1;
    }
}