<?php

use phpDocumentor\Reflection\Types\Boolean;

class Q21
{
    /**
     * 引数として要素数が基数のint型配列を与えられ、与えられた配列の
     * 真ん中3つを要素とする配列を作成し返す
     * 与えられた配列の要素は3つ以上
     * 例：
     * 　midThree([1, 2, 3, 4, 5]) → [2, 3, 4]
     * 　midThree([8, 6, 7, 5, 3, 0, 9]) → [7, 5, 3]
     * 　midThree([1, 2, 3]) → [1, 2, 3]
     * 
     * @param int [] nums int型の配列
     * 
     * @return int [] 上記の処理の結果のint型配列
     * 
     */
    public function midThree(array $nums) : array
    {
        return [];
    }
}