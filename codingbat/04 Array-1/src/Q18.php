<?php

class Q18
{
    /**
     * 引数として要素数が偶数のint型の配列を与えられ、配列の
     * 真ん中の2要素を要素とする配列を返す
     * 例：
     * 　makeMiddle([1, 2, 3, 4]) → [2, 3]
     * 　makeMiddle([7, 1, 2, 3, 4, 9]) → [2, 3]
     * 　makeMiddle([1, 2]) → [1, 2]
     * 
     * @param int [] $nums
     * 
     * @return int [] 上記の処理で生成されたint型の配列
     * 
     */
    public function makeMiddle(array $nums) : array
    {
        return [];
    }
}