<?php

class Q29
{
    /**
     * 2つの引数(0以上の整数)の1の位の数値が等しい場合はtrueを返す。
     * それ以外はfalseを返す
     * 例：
     * 　　lastDigit(7, 17) → true
     * 　　lastDigit(6, 17) → false
     * 　　lastDigit(3, 113) → true
     * 
     * @param int $a 整数
     * @param int $b 整数
     * 
     * @return 2つの引数(0以上の整数)の1の位の数値が等しい場合はtrueを返す。
     * 　　　　それ以外はfalseを返す
     * 
     */
    public function lastDigit(int $a, int $b)  : bool
    {
        return false;
    }
}